{
  "name": "connected-web-tasklist",
  "description": "A simple PHP, HTML and JavaScript based task list viewer",
  "version": "0.1.0",
  "private": true,
  "license": "ISC",
  "author": "John Beech",
  "engines": {
    "node": ">=4.3.2"
  },
  "scripts": {
    "start": "node app.js",
    "test": "mocha test/**/*.js --recursive",
    "setup": "./scripts/setup/installComposer.sh && cd web/auth && php composer install"
  },
  "dependencies": {},
  "devDependencies": {
    "assert": "^1.4.1",
    "express": "^4.14.1",
    "mocha": "^3.2.0"
  }
}
